# API base URL for fetch() calls (no trailing slash).
# Copy to frontend/.env.local and change for cloud API testing.
VITE_API_URL=http://localhost:8080

# Local React UI + API on EC2 (example — use your EC2 public IP):
# VITE_API_URL=http://107.21.84.223:8080
