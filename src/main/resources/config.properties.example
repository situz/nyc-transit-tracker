# Copy this file to config.properties and set your key (config.properties is gitignored).
# Or set environment variable MTA_API_KEY instead — that takes precedence.
mta.api.key = YOUR_MTA_API_KEY_HERE
